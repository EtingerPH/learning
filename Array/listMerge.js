const alunos1 = ["Anna", "Julia", "Zeus", "Negão"]

const alunos2 = ["Cynthia", "Luiza", "Luluca", "Negrudinha"]

const merge = alunos1.concat(alunos2, "Chester")

//console.log(`a lista possui ${merge.length} nomes \n ${merge}`)
console.log (merge)

//No método concat() é possível passar uma informação como parâmetro que será incluída no array
//e não somente um novo array.
